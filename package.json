{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "rirc",
  "title": "RIRC",
  "description": "Search and paste GIFs from GIPHY or KLIPY.",
  "icon": "icon.svg",
  "author": "2Ahmad5",
  "license": "MIT",
  "commands": [
    {
      "name": "index",
      "title": "Search GIFs",
      "description": "Search GIFs from GIPHY or KLIPY.",
      "mode": "view",
      "preferences": [
        {
          "name": "giphyApiKey",
          "type": "password",
          "required": false,
          "title": "GIPHY API Key",
          "description": "API key from developers.giphy.com.",
          "placeholder": "Enter GIPHY API key"
        },
        {
          "name": "klipyApiKey",
          "type": "password",
          "required": false,
          "title": "KLIPY API Key",
          "description": "App key from the KLIPY Partner Panel.",
          "placeholder": "Enter KLIPY app key"
        },
        {
          "name": "jinaApiKey",
          "type": "password",
          "required": false,
          "title": "Jina AI API Key",
          "description": "API key for visual semantic search of saved media.",
          "placeholder": "Enter Jina AI API key"
        }
      ]
    }
  ],
  "scripts": {
    "build": "ray build -e dist",
    "dev": "ray develop"
  },
  "dependencies": {
    "@raycast/api": "^2.2.0"
  },
  "devDependencies": {
    "@types/node": "22.19.17",
    "@types/react": "19.0.10",
    "typescript": "^6.0.3"
  }
}
